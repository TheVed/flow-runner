# Shared

Shared TypeScript types and utilities used by the API, web app, and worker.

Planned responsibilities:

- Workflow graph types
- Workflow run state types
- Step execution state types
- Shared validation schemas
